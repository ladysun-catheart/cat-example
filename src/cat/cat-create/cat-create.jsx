import React from 'react';
import CatForm from '../cat-form';

const CatCreate = () => {
    const handlerSend = () => {};
    return (
        <CatForm btnName={'Insert'} />
    );
}

export default CatCreate;